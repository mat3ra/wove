import React from "react";
import { NodeData } from "../../units/types";
interface Props {
    data: NodeData;
}
declare function DefaultNode(props: Props): React.JSX.Element;
export default DefaultNode;
